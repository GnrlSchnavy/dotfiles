# Package Management Strategy

**Moved.** The canonical version of this document is
[`docs/packages.md`](../docs/packages.md) — decision matrix
(Nix vs Homebrew vs Mac App Store), language-runtime policy,
add/remove workflow, and the `cleanup = "zap"` warning.

This stub remains so existing links don't break.
