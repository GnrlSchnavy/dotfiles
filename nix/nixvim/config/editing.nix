{ ... }:
{
  plugins = {
    # Git change markers in the gutter (added/changed/removed lines)
    gitsigns.enable = true;

    # Auto-close brackets, quotes, etc.
    nvim-autopairs.enable = true;
  };
}
